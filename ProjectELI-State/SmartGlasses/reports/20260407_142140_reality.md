# Reality cycle

## Inputs Used This Cycle
- core/field_v2/attractors.json
## Reality Check

### Subtitle Placement: feasible now
- **Reasoning**: The current focus is on real-time subtitle clarity, and the `TranscriptLab` app provides a clear test bed for this feature. The initial placement of subtitles can be refined as long as it remains readable in motion and noise. Given the simplicity requirement for V1 interaction (frame-touch only), subtitle placement should prioritize usability without becoming overly complex.

### Confidence Display: feasible now
- **Reasoning**: The `TranscriptLab` app already includes confidence display, which is a critical component of real-time subtitles. Enhancing this to show confidence levels or labels can be done incrementally as long as it does not create unnecessary complexity or visual clutter.

### Memory/Cache Policy: feasible now
- **Reasoning**: The current implementation in `TranscriptLab` allows for basic memory caching and retrieval, which aligns with the initial V1 requirements. Incremental improvements to how names and faces are stored and reinforced can be made without major architectural changes.

### Phone/Cloud Boundary: feasible later
- **Reasoning**: While the project docs emphasize a phone-first approach, this boundary will need more detailed consideration as the system evolves. For now, focusing on refining subtitle clarity and memory support is sufficient, with potential future expansion into cloud-based features.

### Visual Hierarchy: feasible now
- **Reasoning**: The visual hierarchy can be tested in `TranscriptLab` to ensure subtitles are easily readable without causing distraction. Early iterations should focus on clarity over feature density to maintain a low-friction user experience.

## Current Open Questions and Decisions

### Subtitle Placement
- **Current State**: Use best effort within the `subtitle placement`.
- **Next Steps**: Test various subtitle placements in the `TranscriptLab` app and ensure they remain readable and unobtrusive. Focus on maintaining a clear, stable display that doesn't distract the wearer.

### Confidence Display
- **Current State**: No specific operator choice selected; use best effort within `confidence display`.
- **Next Steps**: Implement basic confidence labels in the `TranscriptLab` app to provide immediate feedback to the user about the reliability of the subtitles. This can be refined over time as more data becomes available.

### Memory/Cache Policy
- **Current State**: No specific operator choice selected; use best effort within `memory/cache policy`.
- **Next Steps**: Implement a simple caching mechanism for names and faces in the `TranscriptLab` app to ensure that relevant information is readily accessible. This can be expanded based on user feedback and performance testing.

### Phone/Cloud Boundary
- **Current State**: No specific operator choice selected; use best effort within `phone/cloud boundary`.
- **Next Steps**: Continue to develop the phone-side processing in the `TranscriptLab` app while keeping cloud usage optional for now. Future iterations can explore integrating more cloud-based features as needed.

### Visual Hierarchy
- **Current State**: No specific operator choice selected; use best effort within `visual hierarchy`.
- **Next Steps**: Ensure that subtitles are easily readable and not distracting in the `TranscriptLab` app. This involves testing different visual elements and layout options to find a balance between clarity and simplicity.

## Reality Check Summary

### Subtitle Placement: feasible now
- Ensuring subtitles remain clear, stable, and unobtrusive in noisy environments is within reach using the existing `TranscriptLab` setup.
  
### Confidence Display: feasible now
- Basic confidence labels can be implemented to provide immediate feedback on subtitle reliability.

### Memory/Cache Policy: feasible now
- A simple caching mechanism for names and faces can be tested and refined based on user interaction data.

### Phone/Cloud Boundary: feasible later
- Early focus should remain on phone-side processing, with potential future expansion into cloud-based features as needed.

### Visual Hierarchy: feasible now
- Ensuring subtitles are readable without causing distraction is achievable within the `TranscriptLab` app.

By following these steps and continuously refining the implementation based on real-world testing, the project can effectively address the core attractors while adhering to the given constraints.